services:
  mongo:
    image: mongo:7
    container_name: claims-management-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: claims_management
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
